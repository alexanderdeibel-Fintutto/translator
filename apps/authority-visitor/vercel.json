{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run build:authority-visitor",
  "outputDirectory": "../../dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
