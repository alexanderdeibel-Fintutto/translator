{
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run build:enterprise",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
